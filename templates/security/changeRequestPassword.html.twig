{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} réinitialiser votre mot de passe{% endblock %}

{% block body %}
        <div class="container">
            <h2 class="text-center my-5 text-secondary">Demande de nouveau mot de passe</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="button d-flex justify-content-center mt-5">
                <button class="btn btn-lg btn-secondary" type="submit">Envoyer</button>
            </div>
            {{ form_end(form) }}
        </div>
{% endblock %}
