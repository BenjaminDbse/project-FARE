<form action="{{ path('graph_recorder',{'id' : import.id}) }}" method="POST">
    <div class="d-flex flex-column align-items-center">
        <label for="date" id="date">Heure de début :</label>
        <select class="mb-3" name="date" id="date">
            {% for date in dates %}
                <option class="bg-secondary text-light"
                        value="{{ date }}"{{ date == startDate ? 'selected' : '' }}>{{ date }}</option>
            {% endfor %}
        </select>
        <label for="toDate" id="toDate">Heure de fin :</label>
        <select class="mb-3" name="toDate" id="toDate">
            {% for date in dates %}
                <option class="bg-secondary text-light"
                        value="{{ date }}"{{ date == endDate ? 'selected' : '' }}>{{ date }}</option>
            {% endfor %}
        </select>
        <button class="btn btn-dark mb-4">Filtrer</button>
    </div>
</form>